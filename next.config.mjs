/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // Exportation statique
  trailingSlash: true, // Ajoute un slash aux URL pour compatibilité Netlify
};
export default nextConfig;

